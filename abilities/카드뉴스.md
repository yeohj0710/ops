# 카드뉴스

- **무엇에 쓰나**: 인스타에 올릴 카드뉴스와 이벤트 포스터를 만든다. 나눔, 공지, 정보 카드 전부
- **제어층**: L3 (레퍼런스 찾기, 이미지 뽑기), L1 (Figma MCP 로 조립)
- **못 하는 것**: 없는 제품을 지어내면 안 된다. 실제 제품은 공식 컷을 찾아 쓰고, 생성은 마지막 수단이다

---

## 순서

**맨손으로 디자인하지 마라.** 머리에서 뽑은 레이아웃은 열에 아홉 "AI 티 난다" 로 반려된다.
같은 상황의 실제 인스타 게시물을 먼저 찾고, 그 판을 그대로 쓴다.

1. **레퍼런스를 찾는다** (아래 절)
2. **뼈대를 글로 적는다.** 무엇을 맨 위에 두고 무엇을 크게 할지
3. **이미지를 뽑는다** (아래 절)
4. **피그마에서 한 장씩 짓고 매번 눈으로 본다** (`피그마.md` 의 규칙을 따른다)
5. **PNG 를 4x 로 뽑아 드라이브 업무 폴더에 둔다** (아래 "PNG 내보내기는 4x")

## 레퍼런스 찾기

크롬으로 인스타 태그 페이지를 연다. 로그인은 이미 돼 있다.

```
https://www.instagram.com/explore/tags/<태그>/
```

상황에 맞는 태그를 고른다. 나눔이면 `팔로워이벤트` `무료나눔` `추첨이벤트`,
정보 카드면 `카드뉴스` `건강정보`, 제품이면 `신제품출시` 식이다.
태그로 안 나오면 구글에 `인스타 <상황> 카드뉴스` 로 검색해서 게시물로 들어가도 된다.

**두 건 이상 본다.** 한 건만 보고 만들면 그 한 장을 베끼게 된다.
두세 건에서 공통으로 나오는 것이 그 판의 문법이고, 그것만 가져온다.

썸네일이 안 뜨면 배경 탭이라 그렇다. **휠을 한 번 굴리면 로드된다.**
게시물을 열었는데 사진이 검게 나오면 잠깐 기다렸다가 다시 캡처한다.

### 나눔, 이벤트 게시물의 공통 문법 (260820 실측)

- 맨 위에 작은 영문 라벨 하나 (`FOLLOWER EVENT`, `BEST SELLER`). 자간을 넓게 준다
- 그 아래 큰 국문 제목 두 줄. 한 어절만 포인트색으로 뺀다
- 제품 사진을 크게. **배경을 지운 것처럼** 깔린다
- `참여 방법` 같은 진한 알약 라벨 + 흰 카드 안에 번호 매긴 세 줄
- 맨 아래 기간과 발표일 한 줄, 그 밑에 계정 핸들
- 배경은 파스텔 크림 한 가지. 얇은 안쪽 테두리 프레임을 두르기도 한다

## 후킹과 강조

- **가장 센 사실을 제목으로 올린다.** 완판, 판매량, 수상, 몇 년 차 같은 것이 여기 해당한다.
  이걸 하단 회색 작은 글씨로 깔면 아무도 안 본다. 260820 에 "현대백화점 팝업마다 완판" 을
  아래 캡션에 뒀다가 "왜 강조를 안 하냐" 고 반려받았다
- **제목 강조는 한 화면에 한 군데.** 제목에서는 어절 하나만 포인트색 + Bold 로 뺀다. 여러 곳을 칠하면 아무 데도 안 보인다
- **본문은 문장마다 한두 어절을 뺀다(260915).** 본문이 전부 같은 굵기와 색이면 "폰트가 밋밋하다, 강조가 아예 없다" 고 반려된다. 성분명과 함량, 고민, 쓰는 때를 굵기와 색으로 뺀다.
  미미팜은 밝은 판 Pretendard Bold `#C8412B`, 어두운 사진 판 Bold 민트 `#BFE3DA`(경고 말은 복숭아 `#F2A47E`), 어라운드팜 제품 카드는 Noto Sans KR Bold `#0E7490`.
  색이 든 라벨(`Pharmacist's Tip:`) 바로 뒤 말은 칠하지 않는다. 라벨과 한 덩어리로 읽힌다. 절차는 `C:\dev\ops\manuals\card-news\MANUAL.md` 5-2 절
- **숫자는 배지로 뽑는다.** `30명` `2000` 처럼 원형 배지에 넣으면 눈이 먼저 간다
- **제목은 공식적인 명사구로 쓴다.** "안에 든 건 세 가지" 는 구어체라 반려됐고 "주요 성분 3가지" 로 고쳤다.
  카드 제목은 말하듯 쓰지 말고 라벨처럼 쓴다. 본문은 반대로 말하듯 쓴다
- 슬로건과 번역투는 그대로 AI 티가 된다. `copy-tone` 규칙을 따른다

## 컴팩트하게

**슬라이드를 늘리지 마라.** 길면 안 넘긴다.

- 나눔과 이벤트는 **3장이 상한**이다. 1장에 이벤트 정보를 다 넣고 나머지 둘은 붙임이다
- 정보 카드뉴스도 6장을 넘기지 않는다
- 한 장에 한 가지만 말한다. 두 가지를 넣고 싶으면 하나를 버린다
- 260820 에 6장으로 만들었다가 3장으로 줄였다. 줄이고 나서 더 좋아졌다

## 이미지 뽑기

### 순서를 지켜라. 생성은 맨 마지막이다

**실제로 있는 제품, 장소, 사람이면 먼저 찾아본다.** 없는 걸 그려내는 건 마지막 수단이다.
260820 에 올레씨를 그려서 만들었다가 "구글에 치면 공식 제품 이미지가 있는데" 라고 지적받았다.

1. **구글 이미지에서 공식 컷을 찾는다.** `<제품명>`, `<브랜드> <제품명> 상세페이지` 로 친다.
   브랜드 자사몰, 스마트스토어, 쿠팡, 백화점몰 상세페이지에 배경 지운 누끼컷이 거의 다 있다
2. 구도가 안 맞으면 **그 이미지를 ChatGPT 에 올려서 원하는 구도로 가공시킨다.**
   실물 그대로 두고 배경, 각도, 소품, 조명만 바꾸는 거라 제품이 안 틀어진다
3. **아무 데도 없을 때만 처음부터 생성한다.** 이때는 보고에 "생성 이미지" 라고 반드시 적는다

크롬으로 찾은 이미지를 받을 때는 아래 "뽑은 이미지를 피그마로 옮기기" 의 캔버스 방식을 그대로 쓴다.
원본 URL 을 반환값으로 꺼내려 하지 마라. 도구가 막는다.

### 있는 이미지를 ChatGPT 로 가공시키기

새 채팅에 이미지를 올리고 무엇을 유지하고 무엇을 바꿀지 나눠 적는다.

```
올린 사진의 제품은 모양, 색, 라벨, 비율을 그대로 유지해줘. 라벨 글자도 바꾸지 마.
배경만 아주 연한 크림색 단색으로 바꾸고, 병 옆 바닥에 레몬 반쪽과 올리브 몇 알을 놓아줘.
정사각형, 정면 각도, 부드러운 자연광, 그림자는 아래로 은은하게만.
```

**"그대로 유지" 를 안 적으면 제품을 새로 그려 버린다.** 라벨 글자도 같이 못 박아야 한다.
가공본을 받으면 원본과 나란히 놓고 라벨과 병 모양이 같은지 눈으로 본다. 틀어졌으면 다시 시킨다.

파일 올리기는 `file_upload` 가 로컬 경로를 거부할 때가 있다. 그때는 `browser-file-upload-workaround`
(postMessage) 를 쓴다.

### 처음부터 생성해야 할 때

**크롬에 로그인된 ChatGPT 로 뽑는다.** 사람이 쓰는 유료 계정이라 따로 돈이 안 나간다.

새 탭을 만들어 `chatgpt.com` 을 열고 **새 채팅**을 누른다.
사람이 쓰던 대화에 이어서 쓰면 그 대화가 오염된다.

프롬프트에 넣을 것.

- **각각 개별 이미지로 몇 장** 이라고 못 박는다. 안 그러면 콜라주 한 장으로 합쳐 준다
- 비율(4:5 세로, 정사각형)
- **배경색을 지정한다.** "아주 연한 크림색 단색 하나만" 처럼. 나중에 슬라이드 배경과 맞추기 쉽다
- **글자와 라벨 문구는 절대 넣지 마.** 안 적으면 엉터리 글자를 그려 넣는다
- **물방울과 기포는 넣지 마.** 사람이 싫어하는 질감이다
- "광택 3D 렌더링이나 클립아트 말고 실사 사진 느낌으로"

### 프롬프트 입력은 스크립트로 넣는다

**긴 한국어를 `computer` 의 `type` 으로 치면 글자가 깨지고 공백이 사라진다.** 260820 실측이다.
입력창이 ProseMirror 라 값 대입도 안 먹는다. `execCommand` 로 넣는다.

```js
const ed = document.querySelector('#prompt-textarea') || document.querySelector('[contenteditable="true"]');
ed.focus();
document.execCommand('selectAll', false, null);
document.execCommand('delete', false, null);
document.execCommand('insertText', false, "여기에 프롬프트 전문");
ed.textContent.length   // 길이가 맞는지 확인하고 Enter
```

### 뽑은 이미지를 피그마로 옮기기

세 갈래를 다 막혀 보고 남은 길이 이거다. **아래 순서 그대로 하면 된다.**

1. `upload_assets` 로 필요한 수만큼 업로드 주소를 받는다 (10분 만료, 한 번만 쓴다)
2. 페이지 안에서 이미지를 캔버스에 그려 blob 으로 만들고 다운로드 폴더에 떨군다

   ```js
   const imgs=[...document.querySelectorAll('img')].filter(i=>/oaiusercontent|backend-api\/(files|estuary)/.test(i.src));
   const seen=new Set(), uniq=[];
   for(const i of imgs){ if(!seen.has(i.src)){ seen.add(i.src); uniq.push(i); } }   // 전체 src 로 걸러야 한다
   for(let i=0;i<uniq.length;i++){
     const im=new Image(); im.crossOrigin='anonymous';
     await new Promise((ok,no)=>{im.onload=ok; im.onerror=no; im.src=uniq[i].src;});
     const c=document.createElement('canvas'); c.width=im.naturalWidth; c.height=im.naturalHeight;
     c.getContext('2d').drawImage(im,0,0);
     const b=await new Promise(r=>c.toBlob(r,'image/png'));
     const a=document.createElement('a'); a.href=URL.createObjectURL(b); a.download=`shot-0${i+1}.png`;
     document.body.appendChild(a); a.click(); a.remove();
     await new Promise(r=>setTimeout(r,700));
   }
   ```

3. 다운로드 폴더에서 curl 로 올린다. 응답에 `imageHash` 가 온다

   ```bash
   curl -s -X POST "<submitUrl>" -F "file=@shot-01.png;type=image/png"
   ```

4. 그 해시를 `{type:'IMAGE', imageHash, scaleMode:'FILL'}` 로 노드 fill 에 넣는다
5. 업로드가 캔버스에 만들어 놓은 원본 프레임은 `x=-1500` 쯤으로 밀어 두고 이름 앞에 `소스` 를 붙인다
6. **작업이 끝나면 다운로드 폴더의 임시 PNG 를 지운다**

### 사진이 네모 상자로 보일 때

생성 이미지 배경과 슬라이드 배경이 다르면 사진이 상자로 뜬다. 오려붙인 것처럼 보이게 하려면 이렇게 한다.

```bash
python -c "
from PIL import Image
im=Image.open('shot-01.png').convert('RGB'); w,h=im.size
for p in [(5,5),(w-6,5),(5,h-6),(w-6,h-6)]: print(p,'#%02X%02X%02X'%im.getpixel(p))"
```

- 나온 값을 **슬라이드 배경색으로 그대로 쓴다.** 그리고 사진의 `cornerRadius` 를 0 으로 둔다
- 사진이 여러 장이라 배경 톤이 서로 다르면, 한 장을 기준으로 잡고 나머지는
  `ImagePaint` 의 `filters` 로 맞춘다. `{exposure:0.07, contrast:-0.03, temperature:0.05}` 정도면 붙는다

**공식 컷을 쓸 때는 반대로 간다. 팔레트를 사진에 맞추지 말고 사진을 팔레트에 맞춘다.**
260820 에 공식 컷 배경(`#EEEAE3`)에 맞춰 슬라이드를 회색으로 바꿨다가 "아까 색이 나았다" 로 되돌렸다.
쓰던 배경색이 더 예쁘면 그걸 지키고, **사진 쪽을 PIL 로 물들여서** 올린다.

```python
from PIL import Image
im = Image.open(src).convert("RGB")
r, g, b = im.split()
r = r.point(lambda v: min(255, int(v * 249 / 238 + 0.5)))   # 238 -> 249 (원본R -> 목표R)
g = g.point(lambda v: min(255, int(v * 239 / 234 + 0.5)))
Image.merge("RGB", (r, g, b)).save(dst)
```

채널마다 `목표값 / 원본값` 을 곱하면 된다. 제품 색도 같이 몇 퍼센트 움직이지만 눈에 안 띈다.
바꾼 파일을 새로 업로드하고 해시를 갈아끼운다. **원본도 같이 남겨 둔다.**

**한글이 든 경로를 `python -c` 로 넘기면 깨진다.** 스크립트 파일로 써서 실행한다.

### 제품이 사진 안에서 작을 때

공식 상세컷은 여백이 넓어서 그대로 넣으면 제품이 작게 나온다. `scaleMode` 를 `CROP` 으로 바꾸고
`imageTransform` 으로 제품만 파고든다. 행렬은 `[[가로비율,0,가로시작],[0,세로비율,세로시작]]` 이고
값은 0에서 1 사이 비율이다.

```js
ph.fills=[{type:'IMAGE', imageHash:HASH, scaleMode:'CROP',
  imageTransform:[[0.690,0,0.163],[0,0.811,0.127]]}];   // 가로 69%, 세로 81% 만 보여준다
```

**사진 틀의 가로세로 비를 잘라낼 영역에 맞춰라.** 세로로 긴 제품이면 틀도 세로로 길게(400x470 식) 잡는다.
정사각 틀에 세로 제품을 넣으면 좌우 여백만 늘어난다.

## 오시는 길 지도 카드 (260911 미미팜)

레퍼런스가 지도 한 장 안내면 **지도 앱을 캡처하지 말고 OpenStreetMap 데이터로 그린다.** 출처 표기만 하면 쓸 수 있고, 도로가 실제와 맞는다.

1. **역 출구와 거리부터 확인한다.** 네이버 `m.place.naver.com/place/{id}/home` 의 HTML 안 JSON `"road"` 필드에 "신당역 10번출구에서 348m" 같은 찾아가는 길이 통째로 있다. 화면 글은 "..." 로 잘린다. 카카오 place 페이지는 `출구정보` 에 도보 분을 준다
2. **Overpass 로 도로를 받는다.** `overpass-api.de` 는 504 로 죽었고 `overpass.kumi.systems` 가 받아 줬다. bbox 하나에 `way[highway]`, `node[railway=subway_entrance]`(ref 가 출구 번호), `node[railway=station]`, `way[railway=subway]` 를 같이 받는다
3. **도보 경로는 `routing.openstreetmap.de/routed-foot/route/v1/driving/경도,위도;경도,위도?overview=full&geometries=geojson&steps=true`.** 거리와 꺾는 곳이 나와 캡션 길 안내에도 쓴다. 꺾는 길 이름은 경로 점에서 가까운 way 의 name 으로 확인한다. 제일 가까운 것은 대개 이름 없는 인도라 두세 번째까지 본다
4. **파이썬으로 판 픽셀에 투영해 SVG 를 만든다.** 건물을 판 안 한 점에 고정하고 1m 에 1.25px 정도. 등급별 굵기(primary 46, secondary 36, tertiary 28, residential 16)마다 `<path>` 하나로 묶으면 노드 7개로 끝난다. 터널, 지하 보도, footway, service 는 뺀다
5. `figma.createNodeFromSvg` 로 넣고 stroke 색만 바꾼다. 지도 판은 `clipsContent` 프레임이다. 역 막대와 역 이름 알약은 `relativeTransform` 으로 선로 방향에 맞춰 돌린다. 방향은 역 반경 안 선로 점들의 주성분 각도로 구한다
6. **판 구석에 `© OpenStreetMap` 을 작게 넣는다.** ODbL 표기 조건이다
7. **경로는 카카오맵 도보 길찾기와 대조한다.** 구글 지도는 한국에서 도보 길찾기를 안 해 준다("도보 경로 검색 서비스 범위를 벗어난 검색"). 게다가 출발지에 "신당역 10번출구" 를 치면 약수역 10번 출구로 잡혔다. `https://map.kakao.com/link/by/walk/출발이름,위도,경도/도착이름,위도,경도` 를 Playwright 헤드리스로 열면 경로 그림과 "6분 392m" 가 나온다. 사거리 이름(흥인사거리 같은)도 여기서 얻는다
8. **"구글맵처럼" 을 시키면 이 결로 간다.** 판 #F1F3F4, OSM 건물 윤곽 #E3E5E8, 흰 도로 + #D2D5D9 테두리(굵기 +3), 도로 이름 #5F6368 흰 테두리 글자, 도보 경로는 파란 #1A73E8 점선(굵기 9, dash [1, 13], 둥근 끝), 출구 번호는 노란 #FBBC04 네모, 목적지는 빨간 핀 #EA4335. 몇 번 출구, 어디서 꺾는지, 어느 입구인지 번호 칩으로 단다. 폴리곤은 Douglas-Peucker 로 줄이지 마라. 닫힌 고리는 시작점과 끝점이 같아 두 점으로 무너진다

## 카드 안 이모지와 아이콘

피그마 글자 이모지는 PNG 로 안 찍힌다. 그렇다고 **3D 이모지 PNG(Fluent Emoji 3D 같은 것)를 이미지로 박지 마라.**
260911 미미팜 약국 소개 카드에 반짝이, 체크, 3D 건물, 알약, 돈주머니를 Fluent 3D 로 넣었다가 "AI 티가 난다" 는 말을 들었다.
반짝이(✨)는 요즘 AI 기호로 읽힌다. 아이콘은 Lucide 선 아이콘을 강조색 한 색으로 넣는다(`피그마.md` 의 Lucide 받는 법).
같은 카드에서 세리프 제목(Abril Fatface), 한글 세리프 머리글(Noto Serif KR Black), 글자 그림자도 "폰트가 구리다" 로 반려됐다. 글꼴은 Pretendard 한 벌로 간다.
레퍼런스에 실제 건물 3D 그림이 있으면 스톡 아이콘으로 때우지 말고, 촬영본을 보고 단순화한 벡터 그림을 짓는다.

## 레퍼런스 좌표에 글자 맞추기

`TextNode.absoluteRenderBounds` 가 글자 잉크 상자다. 레퍼런스 PNG 에서 numpy 로 잰 잉크 상자(왼쪽 끝, 윗선, 세로 중심)에 그대로 맞춘다.
제목 폭은 `크기 x 목표폭 / 잉크폭` 을 두세 번 되풀이해야 맞는다. 한 번으로는 30px 가까이 어긋났다. 그림자 효과를 켠 채 재면 상자가 부푸니 끄고 잰다.

## 언어판 줄 만들기 (KR 최종본 복제)

사용자가 KR 줄을 최종으로 고친 뒤 "중 일 영 밑으로" 라고 하면 이렇게 한다. 260911 미미팜 S1 에서 한 방식이다.

1. KR 프레임과 캡션 칸을 `clone()` 해서 EN y +1550, CN y +3100, JP y +4650 에 같은 x 로 놓고 이름의 `-KR-` 을 바꾼다. KR 줄은 건드리지 않는다
2. 글꼴은 EN Pretendard, CN Noto Sans SC, JP Noto Sans JP. **Noto Sans SC, JP 에는 SemiBold 와 ExtraBold 가 없다.** Medium, Black 으로 옮긴다
3. 글을 갈면 폭이 바뀐다. 바꾸기 전 잉크 상자(`absoluteRenderBounds`)의 왼쪽 끝과 세로 중심을 재 두고, 바꾼 뒤 그 자리로 되돌린다. 자동 레이아웃 칩은 기준 모서리(오른쪽 끝, 가운데)로 다시 놓고, 회전한 도로 이름은 중심과 각도로 다시 놓는다
4. 칸을 넘으면 글자를 줄이되, **캐러셀 장끼리 같아야 하는 제목 크기는 줄이지 말고 번역을 짧게 바꾼다.** 후보 문구를 여러 개 두고 원래 크기에서 들어가는 첫 문구를 고른다. 260911 에 영어 3장 제목이 45.9 로 줄어 2장(56)과 달라 보였다
5. 캡션은 KR 캡션 칸을 복제해 라벨을 `영어 캡션` 등으로 바꾸고, 본문 전체를 그 언어 글꼴 Regular 로 덮은 뒤 `1️⃣` 키캡만 Noto Emoji 로 바꾼다. 스크립트 안 이모지는 `'\u{1F5FA}️'` 처럼 코드포인트로 적는다
6. 표기와 말투는 같은 계정에서 이미 끝난 언어판 캡션(미미팜은 R1)을 따른다. 영업시간 같은 사실이 KR 안에서 엇갈리면 옮기기 전에 짚는다
7. 줄마다 `screenshot` 으로 보고, 글이 프레임 밖으로 나간 노드를 `absoluteRenderBounds` 로 훑는다. 캡션 원문을 반환값으로 통째로 돌려받으면 20KB 를 넘어 응답이 깨지니 길이와 해시만 받거나 두 언어씩 코드포인트 배열로 받는다

## PNG 내보내기는 4x

**카드 PNG 는 최소 4x 로 뽑는다.** 사용자(260911): "내보내기는 최소 4x로 부탁해. 2x로도 이미지 화질이 깨지더라". 1080x1350 카드면 4320x5400 이다. 1x, 2x 사본을 업로드 자리에 두지 않는다.

- Figma MCP `download_assets(fileKey, nodeId, defaultFormat 'png', defaultScale 4)` 로 프레임마다 받는다. 4 가 최대값이다. 12장을 한 번에 병렬로 불러도 됐다. 응답 URL 은 10분 안팎만 살아서 받자마자 `curl -L -o` 로 내려받고 PIL 로 크기를 잰다
- `get_screenshot` 은 노드 원래 크기보다 크게 안 그린다. 내보내기에 쓰지 않는다
- 사진이 꽉 찬 장은 한 장에 16~19MB, 벡터 위주 장은 3MB 안팎이다
- 사진이 흐리면 배율과 별개로 이미지 채우기 원본 픽셀이 모자란 것이다. 원본을 보이는 크기의 3배 이상으로 넣는다
- 어라운드팜, 미미팜 폴더는 260915 부터 계정 드라이브 `카드뉴스\<번호 2자리> <YYMMDD> <제목>\<KR|EN|CN|JP>\` 에 `01.png` 부터와 `캡션.txt` 만 둔다. 번호와 날짜는 `G:\내 드라이브\에이전트\기억\SNS 계정 운영\카드뉴스\게시 목록.json` 이 원본이고, 경로는 같은 폴더 `도구\cardnews_folders.py folder <브랜드> <벌>` 로 받는다. 제작 노트, `캡션 EN.txt` 같은 파일, 날짜 붙은 새 폴더는 만들지 않는다. 끝나면 `check`, `index` 를 돌리고 폴더 경로를 코드블록으로 준다. 규칙은 `C:\dev\ops\manuals\card-news\MANUAL.md` 9절

## 알려진 함정

- **`figma.createAutoLayout()` 은 흰 fill 이 기본이다.** `fills=[]` 를 안 하면 글자 줄마다 흰 띠가 깔린다.
  260820 에 첫 슬라이드가 통째로 이것 때문에 망가졌다
- **크롬에서 이미지 URL 을 반환값으로 받으려 하면 도구가 막는다.** 쿼리스트링이 자격증명처럼 보여서다.
  URL 을 밖으로 꺼내지 말고 **페이지 안에서 처리를 끝낸다**
- **`chatgpt.com` 에서 `mcp.figma.com` 으로 POST 하면 CORS 로 죽는다.** 이미지 `fetch` 자체는 200 으로 된다.
  그래서 위 절차가 캔버스와 다운로드를 거친다. 페이지에서 바로 올리려는 시도는 다시 하지 마라
- **피그마 `query()` 셀렉터에 한글을 넣으면 파서가 죽는다.** `Invalid selector: unexpected character` 가 뜬다.
  `findOne(n => n.type==='TEXT' && n.characters.indexOf('완판')!==-1)` 로 잡는다
- **`setRangeFills` 인덱스는 줄바꿈과 공백을 다 센다.** 손으로 세면 한 칸씩 어긋난다.
  `const i = node.characters.indexOf('완판')` 로 잡고 `i, i+2` 를 쓴다
- **텍스트를 감싼 알약 프레임은 `primaryAxisAlignItems='CENTER'`** 를 줘야 글자가 가운데로 온다.
  안 주면 폭을 채운 뒤 왼쪽에 붙는다
- **인스타 태그 페이지는 배경 탭에서 이미지를 안 그린다.** 휠을 한 번 굴린다
- **오른쪽 끝 글자가 잘리면** 컨테이너가 `FILL` 인지, 부모 패딩이 88 인지 본다
- 큰따옴표 여는 기호(`“`)는 Pretendard 에서 기울어진 겹따옴표로 그려진다. 인용 부호로 쓰면 그 모양이다
- **구글 드라이브(G:) 위에서 `mv` 로 한글 파일명을 바꾸지 마라.** 260820 에 `cannot stat` 이 뜨면서
  그 폴더의 다른 파일까지 사라졌다. 이름을 바꿔야 하면 **새 이름으로 `cp` 하고 확인한 뒤 지운다.**
  피그마에서 다시 뽑는 게 제일 안전하다

## 작업 끝에

- PNG 는 `<드라이브>/영상 편집/.<대상>/_<건 이름>/` 처럼 그 건 폴더에 모은다
- 피그마 파일 링크를 보고에 같이 적는다. 파일 이름은 만들 때만 정할 수 있다
- **생성 이미지를 쓴 자리는 보고에 명시한다.** 공식 컷을 못 찾은 자리라는 뜻이다
- 날짜, 마감처럼 못 받은 값은 `○월 ○일` 로 두고 무엇이 비었는지 보고한다


## 디자인 소스 폴더 (260914)

**계정 드라이브 폴더마다 `디자인 소스\` 를 둔다.** `[운영] 미미팜 인스타그램\디자인 소스\`, `[운영] 어라운드팜 인스타그램\디자인 소스\`. 안에 `레퍼런스 캐러셀\<코드> <계정> <용도>\`, `제품 공식 이미지\<제품> (<사이트>)\`, `앱 화면\`, `약국 사진\` 과 색인 `README.md`. 카드뉴스를 짓기 전에 여기부터 보고, 새로 받은 원본은 여기에 넣고 README 에 한 줄 늘린다. 사용자(260914): "다음에도 활용할 수 있도록 각 계정 폴더에 모아놓고 그걸 활용해서 디자인해라."

### 인터넷에 이미 디자인된 소스부터 쓴다

사용자(260914): "인터넷에 이미 디자인되고 멋있는 자료 소스들 많으니까 그대로 쓰면 돼. 제품 상세페이지 같은 소스를 잘 취해서 활용해라." 순서는 이렇다.

1. **레퍼런스 원본**: 노션 기획안 "형식:" 링크의 캐러셀을 `에이전트\기억\SNS 계정 운영\카드뉴스\도구 260914\fetch_refs3.py` 로 전 장 받는다(`PYTHONIOENCODING=utf-8 python fetch_refs3.py <출력폴더> <코드…>`). 캡처가 아니라 원본을 잰다.
2. **제품 사진**: 브랜드 공식몰 HTML 을 curl 로 받아 이미지 주소를 뽑는다. Shopify 는 `cdn/shop/files/*.png|jpg`, Cafe24 는 `/web/product/big/…` 과 상세 `/web/upload/NNEditor/…`, 네이버 계열은 대개 JS 라 Playwright. 흰 바탕이면 `rembg` 로 뺀다(설치돼 있다).
3. **앱 화면**: 앱스토어 페이지 HTML 에서 `is1-ssl.mzstatic.com/image/thumb/PurpleSource…/N.png/` 를 뽑고 `1242x2688bb.png` 를 붙이면 마케팅 스크린샷 원본이 온다. 실제 화면은 Playwright 로 430px 폭, `device_scale_factor=3` 으로 캡처한다(`도구 260914/ap_shots.py`).
4. **매장 사진**: 계정 폴더 `제품 데이터\<약국>\` 이나 `약국 사진\`. 없으면 브랜드 자체 인스타 게시물(글자 안 박힌 것)을 받는다.
5. 그래도 없을 때만 생성한다.

### 레퍼런스 좌표를 잰다

`도구 260914/measure.py spec.json` 이 잉크 상자(`rows`), 밝은 글자(`bright`), 특정 색(`color`), 픽셀(`px`)을 한 번에 찍는다. 글자 크기는 잉크 높이로 환산한다(라틴 대문자 0.71, 어센더~디센더 0.97, 한글 0.92). 레퍼런스가 1080 이 아니면(스타일코리안 3274) 비율을 곱한다.

### 피그마 함정 (260914)

- `textAutoResize='HEIGHT'` 직후 읽은 `height` 는 레이아웃 전 값(10)이다. 세로 가운데 배치는 다음 문장에서 `height` 를 다시 읽고 한다.
- `use_figma` 한 호출에 스크린샷은 다섯 장까지만 돌아온다.
- 제품 사진은 이미 잘려 있는 카드의 이미지 노드를 `clone()` 해서 쓴다(CROP 변환이 따라온다).

## 영어 앞세우기와 어라운드팜 통일 (260914 오후)

사용자가 260914 네 벌을 보고 짚은 것이다. 규칙은 `C:\dev\ops\manuals\card-news\MANUAL.md` 10-1, 10-2 절에 있다.

- **"매대" 금지.** "어라운드팜 샵인샵" 이라고 쓴다. 한국에서 매대라는 말을 안 쓴다.
- **제목, 알약, 표 머리글, 제품명, CTA 문장은 영어.** 설명 문장, 훅 부제, 주의 문구, 캡션은 한국어. "웬만한 표현은 영어로, 그래야 감도가 있다."
- **어라운드팜은 채널 문법으로 통일한다.** 클라이언트 담당자가 붙어 있는 계정이다. 밝은 `#F4F3F2` 바탕, 제목 `#1B1917`, 민트 알약에 흰 글, 사진 위 워드마크엔 흰 알약, 마지막 장은 공통 CTA 카드 복제. 근거는 `G:\내 드라이브\[운영] 어라운드팜 인스타그램\디자인\어라운드팜_SNS_디자인방향.pdf`.
- **캡션 칸은 모든 벌에.** 남이 지은 벌도 비면 채운다. 말투는 어라운드팜 A1, 미미팜 S1. 첫 줄 훅, 항목 끝 이모지.
- 글자만 바꿀 때는 `insertCharacters(i, 새 글, 'AFTER')` 다음 `deleteCharacters`. `characters` 통째 교체는 강조와 키캡 글꼴을 푼다.
- 캡션 글에 `U+2028` 이 섞여 있으면 `use_figma` 응답이 깨진다. 읽을 때 BMP 밖 글자와 U+2000~2FFF 를 `[U+..]` 로 바꿔 돌려받는다.

## 한국어 줄바꿈과 미미팜 구매 유도 (260914 저녁)

- **한국어는 단어 단위로 끊는다.** 피그마는 한글을 글자 사이에서 끊는다. 폭 고정 글상자의 넘치는 줄은 단어 앞 빈칸을 `U+2028` 로 바꾼다. 폭은 `WIDTH_AND_HEIGHT` 측정 글상자로 바로 잰다. 캡션 칸은 원문이라 안 건드린다. 절차는 `C:\dev\ops\manuals\card-news\MANUAL.md` 10-3 절.
- **미미팜은 카드마다 `Shop at MIMIPharm 4F` 배지**, 약국 카드에 `이 제품, 여기서 살 수 있어요`(260915 사용자가 R2 에서 줄인 문장), 캡션에 구매 줄. 독점 표현은 금지. 10-4 절.
- **약국 이름은 네 언어 모두 `MIMIPharm` 하나(260915).** 사용자: "미미팜은 MIMIPharm으로 표기 통일해. 다른 언어권도 모두. 이거는 워크플로우에 딱 박아놓고 너도 지켜." 한국어 문장(`MIMIPharm 약사님이`), 캡션, 해시태그(`#MIMIPharm`)까지 영문으로 쓴다. `미미팜`, `ミミファム`, `ミミファーム`, `米米` 같은 음차는 쓰지 않는다. 건물 이름 `미미라인`, `Mimiline`, `ミミライン` 은 그대로. 10-4 절.
- **260916 다시 지적: "MIMIPharm으로 표기형식 통일하랬는데 왜 안함. 에이전트폴더에 규칙세팅해놓고 반드시 지키셈."** "내보낼 때 고친다" 로 미뤄서 R3 에 `미미팜` 이 남았다. 벌을 여는 순간 카드 글과 캡션 칸을 검사해서 그 자리에서 고친다. 검사 코드는 `C:\dev\ops\manuals\card-news\MANUAL.md` 맨 위 "표기 규칙".
- **약사 호칭은 `약사님`(260916, 어라운드팜과 미미팜 한국어 카드, 캡션).** 사용자: "약사라고 하지 말고 약사님이라고 적으삼. 약사님들과 밀접한 채널이라." `약사가 고른` → `약사님이 고른`, `약사 추천` → `약사님 추천`. `약사법`, `약사회`, `약사공론` 같은 고유명사와 검색용 해시태그 `#약사추천` 은 그대로. 드라이브 캡션은 `cardnews_folders.py check` 가 `표기 위반` 으로 센다.
- 함정: `hasMissingFont` 는 글꼴 불러오기 전에 읽으면 참이다. `getStyledTextSegments` 구간 객체에 속성을 붙이면 `object is not extensible`. 정규식 리터럴 안의 `U+2028` 은 문법 오류다.
- 아이콘은 `figma.createNodeFromSvg(SVG 문자열)` 로 바로 벡터가 된다. 알약은 `figma.createAutoLayout` 에 아이콘과 글을 넣고 `cornerRadius = 100`.

## 미미팜 이름과 머리줄, 번역 다듬기 (260914 밤)

- **미미팜 카드 위쪽은 로고 + `MIMIPharm` + `Shop at MIMIPharm 4F` 배지 한 줄(가운데 y 90).** 배지를 맨 위에 붙였다가 "위에 공간이 없다" 지적. 줄을 넣은 만큼 제목과 본문을 내린다. 약국 이름은 `MIMIPharm`, `미미팜` 하나로(`MIMI K-BEAUTY CARE PHARMACY`, `미미 약국`, `MIMI Pharm` 금지). 절차는 `C:\dev\ops\manuals\card-news\MANUAL.md` 10-4 절.
- **언어판은 직역하지 않는다.** "자극이 느껴지는 날 → 刺激を感じる日 는 어디가 따가운지 모른다" 는 일본 쪽 피드백. 주어와 부위(肌, skin)를 넣고, 현지 화장품 문구로 다시 쓰고, 원래 줄 수를 지킨다. 대표 표현 표는 10-5 절.
- 강조가 섞인 글을 바꿀 때: 바꾸기 전 `getStyledTextSegments(['fontName','fontSize','fills'])` 로 가장 긴 스타일과 두 번째 스타일을 잡고, 새 글에 `[[강조]]` 표시를 달아 `characters` 교체 뒤 두 스타일을 다시 입힌다.

## 제품은 계정 목록 안에서만 (260914 밤)

- **어라운드팜은 샵인샵 제품, 미미팜은 미미팜 약국 제품만 쓴다.** 목록은 `G:\내 드라이브\에이전트\기억\SNS 계정 운영\제품 목록 미미팜.md`, `제품 목록 어라운드팜.md`. 사진 경로와 확인한 값, 쓴 벌이 들어 있다.
- 기획안의 타사 제품 비교나 다른 계정 제품은 목록의 같은 성격 제품으로 바꾸고 노션 편집 비고에 적는다. 미미팜 S4 의 톰 크라이오 케어 마스크(샵인샵 브랜드)를 이지듀 EGFx 다운타임 오인트 마스크로 바꾼 것이 첫 사례다.
- **어라운드팜은 가격표 xlsx 26종만 쓴다(260916).** `G:\내 드라이브\[운영] 어라운드팜 인스타그램\shopinshop_제품가격정보_0902.xlsx` 가 원본이다. 사용자: "온슬로 트라넥삼산 기미패치라는 제품은 어라운드팜 리스트에 없음", "카드뉴스에서 다룰 제품 정보는 xlsx 여기에 리스트가 있고 이걸 기반으로 구글에서 제품 데이터 등 조사해서 만들어야 해". 자료 폴더에 누끼만 있는 제품(온슬로 기미, 미간, 아이 패치와 치크팩, 닥터멜락신 하이드로겔 마스크, CJ웰케어)은 260914 에 허용했다가 260916 에 뺐다.
- 가격표 이름과 포장 이름이 다르면 포장 이름을 쓴다(`온슬로 레티놀 팔자주름패치` 는 포장이 레티날, 40매).
- **카드와 캡션에 가격을 쓰지 않는다(260917, 네 언어, 두 계정).** 사용자: "이거 가격을 쓰면 안되지 않아?", "아 이거 가격을 쓰면 안됐는데 큰일났네". `G:\내 드라이브\에이전트\기억\SNS 계정 운영\운영 방식.md` 의 "고객 확인 없는 가격과 할인은 어느 계정에서도 하지 않는다" 를 놓쳐 A4 네 계정 게시물에 가격 알약이 나갔다. 가격표 xlsx 는 제품을 고르는 데만 쓰고, 파는 곳은 `어라운드팜 제휴 약국에서 구매할 수 있어요` 처럼 말한다. 올라간 캐러셀은 그림을 바꿀 수 없어(장 빼기와 순서만 됨) 다시 올려야 한다.
- **문구는 원본 대조 사실표에서 가져온다(260917).** `G:\내 드라이브\에이전트\기억\SNS 계정 운영\카드뉴스\제품 사실 확인 260916\aroundpharm_*_facts.md` 세 개에 17종의 사용법 원문과 "쓰면 안 되는 말" 이 있다. 원본끼리 숫자가 다르면 숫자를 뺀다. 사용 횟수와 때(이틀에 한 번, 시술 당일, 도톰하게)는 원본에 없으면 쓰지 않는다.
- 절차는 `C:\dev\ops\manuals\card-news\MANUAL.md` 2-3 절.

## 문구는 올라간 앞 번호 벌에서 (260916)

- 캡션과 카드 문장을 쓰기 전에 `게시 목록.json` 의 status `게시` 벌 KR 캡션과 카드를 먼저 읽고 그 말투를 따른다. 사용자: "번호가 더 앞에 있는 카드들은 이미 발행, 업로드 완료된 건데 그것들을 참고하라고."
- `🏪 성분이 헷갈릴 때 약사님께 바로 물어볼 수 있는 게 약국에서 사는 이유예요.` 와 그 번역은 쓰지 않는다("AI 티 심해", "도치법이나 수동형 한국에서 표현 잘 안 써"). 구매 줄은 `🏪 세 가지 모두 어라운드팜 약국에서 구매할 수 있어요.` 한 줄이다.
- 어라운드팜 제품 카드 여백 숫자(위 줄, 오른쪽 열 알약 두 개, 라벨 알약 auto layout, 라벨 아래 20, 목록 간격 56)는 `C:\dev\ops\manuals\card-news\MANUAL.md` 10-6, 10-7 절.

## 사용자가 고친 한국어판을 언어판에 옮기기 (260914 밤)

- 노드를 자식 순서로 짝짓고 **세 언어가 서로 같은데 KR 만 다른 속성**을 뽑는다. 그게 사람이 옮기거나 키운 곳이다. 이름으로 짝지으면 글 노드 이름이 글 내용이라 전부 어긋난다.
- 글은 KR 과 EN 을 나란히 읽어 뜻을 대조하고, 줄 구성이 바뀌었으면 언어판도 같은 줄 수로 맞춘다. 후보 문장 여러 개를 측정 글상자로 재서 폭 안에 드는 것을 고른다.
- 자식 수가 다르면(한 언어에만 남은 줄) 먼저 그 노드를 정리해야 순서 짝짓기가 맞는다.
- **"걍 복사해서 번역만 다시" 라고 하면(260915 미미팜 R2)** 옛 언어판 카드 프레임을 백업 페이지로 옮기고 KR 카드를 clone 해 y +1550, +3100, +4650 에 둔 뒤 자식 경로가 같은 글상자만 번역으로 간다. 옛 번역은 KR 뜻이 같으면 다시 쓰고, CN 은 Noto Sans SC, JP 는 Noto Sans JP 로 스타일을 옮긴다(SemiBold→Medium, ExtraBold→Black). 바꾼 뒤 KR 과 높이가 다른 글상자 0건인지 재고 스크린샷으로 본다. 캡션 칸은 그 자리에서 줄만 간다.
- 상세와 R1 사례는 `G:\내 드라이브\에이전트\기억\SNS 계정 운영\카드뉴스 제작.md` 의 같은 이름 절.

## 제품명과 사진을 포장 글자로 대조 (260914 밤)

- 카드에 제품명과 성분을 넣기 전에 그 제품 사진(누끼, 촬영본)을 열어 포장 글자와 맞춘다. 미미팜 S9 에서 이지듀 멜라 글루타치온 크림을 "EGF 멜라토닝 크림" 으로 잘못 썼다.
- 한 벌을 드라이브에 넣을 때는 `G:\내 드라이브\에이전트\기억\SNS 계정 운영\카드뉴스\도구\cardnews_folders.py` 의 `folder`, `captions`, `check` 를 쓴다(260915). 옛 `도구 260914\drive_set.py` 는 벌 폴더에 제작 노트를 넣는 옛 꼴이라 쓰지 않는다.
- 레퍼런스에 사람이 나오면 손과 제품만 나오는 약국 촬영본으로 바꾼다. 상세는 `카드뉴스 제작.md` 의 "260914 밤 미미팜 S9~S12".
