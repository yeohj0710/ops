import fs from 'node:fs/promises';import path from 'node:path';import {execFile} from 'node:child_process';import {promisify} from 'node:util';const run=promisify(execFile);const root=path.resolve(process.argv[2]);const work=path.join(root,'etc/capcut-retouch');const ss=JSON.parse(await fs.readFile(path.join(work,'sources.json'),'utf8'));for(const id of process.argv.slice(3)){const s=ss.find(x=>path.basename(x.relative)===id+'.MOV');const t=Math.max(2,Math.floor(Number(s.probe.format.duration)/2));await run('ffmpeg',['-v','error','-ss',String(t),'-i',path.join(root,'보정 완료',id+'_보정.mp4'),'-vf','fps=2,scale=360:640,tile=3x2','-frames:v','1','-y',path.join(work,'frames',id+'-motion.jpg')],{maxBuffer:2000000});console.log(id+' '+t+'~'+(t+2.5)+'s');}